# Server-Side Request Forgery (SSRF) via Run Health Check
## Vulnerability Type
Server-Side Request Forgery (SSRF)

## Summary
While testing the **Run Health Check** functionality, I noticed that the application accepts user-supplied URLs and fetches them from the server without proper validation.
By providing a local file URI (`file:///etc/passwd`) instead of a normal service URL, I was able to make the server read one of its own internal files and return the contents in the response.
This confirms that the application is vulnerable to SSRF and allows access to local resources that should not be accessible to users.

## Vulnerable Endpoint
`/603.php`

### Parameter
`service_url`

## Steps to Reproduce
1. Go to: `https://kzlabs.com/603.php`
2. Locate the Run Health Check section.
3. In the Service Endpoint URL field, enter: `file:///etc/passwd`
4. Click Check Now.
5. Observe that the application processes the supplied URI and displays the contents of the server's `/etc/passwd` file.

## Payload Used
`
file:///etc/passwd
`

## Proof of Concept
## Screenshot 1
The payload `file:///etc/passwd` is supplied in the Service Endpoint URL field.

<img width="1919" height="942" alt="Screenshot 2026-06-07 181343" src="https://github.com/user-attachments/assets/d7e547da-99db-4323-8ff0-e20aaed95251" />


### Screenshot 2

The application reads and returns the contents of the server's local `/etc/passwd` file.

<img width="1917" height="935" alt="Screenshot 2026-06-07 181400" src="https://github.com/user-attachments/assets/42ef7d0b-79ac-4d60-89c4-27e73cbdba89" />


## Impact
An attacker can make the server access files and resources that should not be publicly accessible.
This may expose sensitive information such as configuration files, credentials, or other internal data.

## Remediation
1. Only allow valid http:// and https:// URLs.
2. Block access to local files and internal resources.
3. Validate user-supplied URLs before processing them.
4. Restrict requests to trusted destinations only.
